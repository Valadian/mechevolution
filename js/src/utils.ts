module bergecraft.rogue{
    export class Utils{
    }
    // export class Array{
    //     static random<T>(self:T[]) {
    //         if (!self.length) { return null; }
    //         return self[Math.floor(ROT.RNG.getUniform() * self.length)];
    //     }

    //     /**
    //      * @returns {array} New array with randomized items
    //      */
    //     static randomize<T>(self:T[]) {
    //         var result:T[] = [];
    //         var clone:T[] = self.slice();
    //         while (clone.length) {
    //             var index = clone.indexOf(Array.random<T>(clone));
    //             result.push(clone.splice(index, 1)[0]);
    //         }
    //         return result;
    //     }
    // }
}